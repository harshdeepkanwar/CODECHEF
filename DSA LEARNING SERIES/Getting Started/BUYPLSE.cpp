#include <iostream>
using namespace std;

int main()
{
    // your code goes here
    int a, b, x, y;
    cin >> a >> b >> x >> y;
    int total;
    total = (a * x + b * y);
    cout << total;
    return 0;
}
